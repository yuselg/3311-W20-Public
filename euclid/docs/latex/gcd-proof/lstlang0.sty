

\lst@definelanguage{Eiffel}
{
morekeywords={agent,alias,all,and,as,is,attached,detachable,check,class,convert,interface,
  creation,create,debug,deferred,do,else,elseif,end,
  ensure,expanded,export,external,false,feature,from,frozen,if,
  implies,indexing,infix,inherit,inspect,invariant,confined,
  like,local,loop,not,obsolete,old,once,or,prefix,
  redefine,rename,require,rescue,retry,select,assign,
  separate,strip,then,true,undefine,unique,until,variant,use,modify,
  when, xor, Result, Current, Void, True, False, across,some,all,loop},
sensitive=false,
showstringspaces=false,
mathescape=true,
morecomment=[l]--,
morestring=[d]",
columns=fullflexible,
breaklines=true,
tabsize=3,
basicstyle=\footnotesize\ttfamily,
backgroundcolor=\color{white},   		% choose the background color
basicstyle=\footnotesize\ttfamily,      % size of fonts used for the code
breaklines=true,                 		% automatic line breaking only at whitespace
captionpos=b,                    		% sets the caption-position to bottom
commentstyle=\color{red},    		    % comment style
escapeinside={\%*}{*)},          		% if you want to add LaTeX within your code
keywordstyle=\bfseries\color{blue},     % keyword style
stringstyle=\color{mygreen},
}[keywords,comments,strings]

% \lstnewenvironment
%   {ecodel}[1][]
%   {\lstset{language=OOSC2Eiffel,float=htb,columns=fullflexible,captionpos=b,
%   xleftmargin=1cm,numberfirstline=true,numberstyle=\hspace*{1cm},numbers=left,mathescape=true,#1}}
%   {}

